server:
  port: 8888

app:
  api-prefix: /savorgo/api

spring:
  application:
    name: API-Gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8080/api/users
          predicates:
            - Path=${app.api-prefix}/**
          filters:
            - StripPrefix=2
logging:
  include-application-name: