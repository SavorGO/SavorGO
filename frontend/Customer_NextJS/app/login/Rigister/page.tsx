"use client";
import Register from "@/components/login/Register/page"; // Kiểm tra đường dẫn đúng với cấu trúc của bạn

const RegisterPage = () => {
  return <Register />;
};

export default RegisterPage;
